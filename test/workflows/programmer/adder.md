# 加法计算器

请实现一个简单的加法计算器程序。

## 功能要求

1. 从标准输入读取两个整数
2. 计算并输出它们的和
3. 处理基本的输入错误

## 技术要求

1. 使用 C++ 实现
2. 代码文件名为 `adder.cpp`
3. 编译后的可执行文件名为 `adder`

## 示例

**输入:**
```
5 3
```

**输出:**
```
8
```

## 实现提示

- 使用 `cin` 读取输入
- 使用 `cout` 输出结果
- 考虑输入错误的处理（虽然不是必须的）
